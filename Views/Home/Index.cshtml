@{
    ViewBag.Title = "Strona Główna";
}

<div class="row">
    <div class="col-md-6">
	@if (User.Identity.IsAuthenticated)
	{
		<p>You're signed in, @User.Identity.Name</p>
	}
	else
	{
		<p>You're not signed in.</p>
	}
        <ul class="list-group">
            <!--<li class="list-group-item">                                         -->
            <!--    <a href="@Url.Action("Index", "Osoby")">Lista Osób (Read)</a>    -->
            <!--</li>                                                                -->
            <!--<li class="list-group-item">                                         -->
            <!--    <a href="@Url.Action("Create", "Osoby")">Dodaj Osobę (Create)</a>-->
            <!--</li>                                                                -->
        </ul>
    </div>
</div>
